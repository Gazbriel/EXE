The button get the user and password. When click on it it gets the Button itself, acces the script "Login action" and then to the method "Login".

Tu use it just drag the button to a canvas. And drag the Iputfields "user" and "password" to the script component on the Button.

The button contains he script for the log in algorithm.

From the inspector, you can set the scenes and the user and pass to acces to every one of them.